# Tajweed review sheet — Surah 1 (Al-Faatiha)

**Reviewer:** Mishary Rashid Alafasy (Hafs, ijazah)  
**Status:** awaiting review — this is an ENGINE DRAFT, not verified.

The text below is the verified Uthmani from the app's bundled mushaf. For each ayah the engine lists the standard Hafs rule **candidates** it detected mechanically. Please **confirm, correct, or strike** each, and **add** any rule the engine missed (madd lengths in particular need your ruling). Your rulings are authoritative.

Sign-off below flips the production gate; until then nothing displays to users as graded.

## 1:1

> بِسْمِ ٱللَّهِ ٱلرَّحْمَٰنِ ٱلرَّحِيمِ

- [ ] **madd (confirm_type_and_length)** on `م` (word 3) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 4) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

## 1:2

> ٱلْحَمْدُ لِلَّهِ رَبِّ ٱلْعَٰلَمِينَ

- [ ] **izhar_shafawi (meem sakinah)** on `م` (word 1) — meem sakinah followed by a non-labial
- [ ] **madd (confirm_type_and_length)** on `ع` (word 4) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 4) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

## 1:3

> ٱلرَّحْمَٰنِ ٱلرَّحِيمِ

- [ ] **madd (confirm_type_and_length)** on `م` (word 1) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 2) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

## 1:4

> مَٰلِكِ يَوْمِ ٱلدِّينِ

- [ ] **madd (confirm_type_and_length)** on `م` (word 1) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 2) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `و` (word 2) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 3) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

## 1:5

> إِيَّاكَ نَعْبُدُ وَإِيَّاكَ نَسْتَعِينُ

- [ ] **madd (confirm_type_and_length)** on `ي` (word 1) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ا` (word 1) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `و` (word 3) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 3) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ا` (word 3) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 4) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

## 1:6

> ٱهْدِنَا ٱلصِّرَٰطَ ٱلْمُسْتَقِيمَ

- [ ] **madd (confirm_type_and_length)** on `ا` (word 1) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ر` (word 2) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 3) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

## 1:7

> صِرَٰطَ ٱلَّذِينَ أَنْعَمْتَ عَلَيْهِمْ غَيْرِ ٱلْمَغْضُوبِ عَلَيْهِمْ وَلَا ٱلضَّآلِّينَ

- [ ] **madd (confirm_type_and_length)** on `ر` (word 1) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 2) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **izhar (noon sakinah)** on `ن` (word 3) — noon sakinah followed by a throat letter ع
- [ ] **izhar_shafawi (meem sakinah)** on `م` (word 3) — meem sakinah followed by a non-labial
- [ ] **madd (confirm_type_and_length)** on `ي` (word 4) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **izhar_shafawi (meem sakinah)** on `م` (word 4) — meem sakinah followed by a non-labial
- [ ] **madd (confirm_type_and_length)** on `ي` (word 5) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `و` (word 6) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 7) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **izhar_shafawi (meem sakinah)** on `م` (word 7) — meem sakinah followed by a non-labial
- [ ] **madd (confirm_type_and_length)** on `و` (word 8) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ا` (word 8) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ا` (word 9) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed
- [ ] **madd (confirm_type_and_length)** on `ي` (word 9) — madd carrier (ا و ي / dagger alif / maddah); type + harakat to be confirmed

---

### Reviewer sign-off

- [ ] I have reviewed every candidate above and the full text of each ayah.
- Corrections/additions noted inline.

Name + credential (ijazah): __________________________  Date: ____________

_Generated candidates: 34. Engine coverage is partial; the reviewer is authoritative._